import React from 'react';
import Logo from './Logo';
import NavLinks from './NavLinks';


const Header = () => {
  return (
    <header>
      <Logo />
      <NavLinks />
    </header>
  );
};

export default Header;
